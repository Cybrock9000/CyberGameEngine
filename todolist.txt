not done = finish the template:
    done = easy map loading with external file or just a one line var
    done but janky = player movement and collision
    done = 2d objects/sprites
    not done = simple raycast line for shooting/interacting
    not done = audio
    done = faster raycasting
    not done = portals?        not going to do it yet
    not done = hud elements
    kinda done = fix sky       works but not top prioraty

not done = finish the engine:
    done = map editor
        kinda done = be able to delete walls
    done = scripts in general
        done = opening them
        done = having them list out in one of the panels
    done = object/sprite creation
    done = list objects and sprites in one of the panels
    not done = compiler
    not done = audio stuff

not done = finish ENGLIB:
    done at this point in time = have all calls to writing to the game run through here
    done = the custom coding language

done for now = finish IDE Editor:
    not done = run code button
    done = scroll
    done = open projects
    done = have engine open projects
    done = shifting in general
    not done = run script in editor
    not done = list files in the file dir

not done = demo project
maybe keep the engine under 1.44mb (put it on a floppy drive some time soon)